const data=window.EVENT_DATA;
const $=(s,r=document)=>r.querySelector(s);
window.changeGallery=(arrow,direction)=>{const card=arrow.closest('.stay-card');const photos=[...card.querySelectorAll('.stay-image>img')];let slide=Number(card.dataset.slide);slide=(slide+direction+photos.length)%photos.length;photos.forEach((img,n)=>img.classList.toggle('gallery-hidden',n!==slide));card.dataset.slide=slide;card.querySelector('.gallery-count').textContent=`${slide+1} / ${photos.length}`};
const teachers=$('#teachers');
data.teachers.forEach((t,i)=>teachers.insertAdjacentHTML('beforeend',`<article class="teacher-card reveal">${t.image?`<img src="${t.image}" alt="${t.name}" loading="lazy">`:`<div class="teacher-placeholder" aria-hidden="true">${t.name.split(' ').map(x=>x[0]).join('')}</div>`}<p class="eyebrow">${t.role}</p><h3>${t.name}</h3><p>${t.text}</p></article>`));
data.program.forEach(d=>$('#program-days').insertAdjacentHTML('beforeend',`<article class="day"><header><div><span>${d.day}</span><b>${d.date}</b></div><span>${d.events.length} событий</span></header>${d.events.map(e=>`<div class="event"><time>${e[0]}</time><p>${e[1]}</p></div>`).join('')}</article>`));
data.accommodations.forEach((a,i)=>{const images=a.images||[a.image];const gallery=images.map((src,n)=>`<img src="${src}" alt="${a.alt}${images.length>1?`, фото ${n+1}`:''}" loading="lazy" class="${n?'gallery-hidden':''}">`).join('');const controls=images.length>1?`<div class="gallery-controls"><button type="button" class="gallery-arrow gallery-prev" aria-label="Предыдущее фото" onclick="window.changeGallery(this,-1)">←</button><span class="gallery-count">1 / ${images.length}</span><button type="button" class="gallery-arrow gallery-next" aria-label="Следующее фото" onclick="window.changeGallery(this,1)">→</button></div>`:'';$('#accommodations').insertAdjacentHTML('beforeend',`<article class="stay-card reveal" data-gallery="${i}" data-slide="0"><div class="stay-image">${gallery}<span>${a.category}</span>${controls}</div><div class="stay-body"><h3>${a.name}</h3><p>${a.text}</p><div class="price"><b>${a.price}</b><span>за участника</span></div><button class="text-button choose" data-value="${a.category} — ${a.name}">Выбрать этот вариант →</button></div></article>`);$('#tariff').insertAdjacentHTML('beforeend',`<option>${a.category} — ${a.name}</option>`)});
data.faq.forEach(([q,a])=>$('#faq-list').insertAdjacentHTML('beforeend',`<details><summary>${q}<span>+</span></summary><p>${a}</p></details>`));
document.addEventListener('click',e=>{const btn=e.target.closest('.choose');if(btn){$('#tariff').value=btn.dataset.value;$('#registration').scrollIntoView({behavior:'smooth'});setTimeout(()=>$('#tariff').focus(),650)}const track=e.target.closest('[data-track]');if(track)window.dataLayer?.push({event:track.dataset.track})});
const params=new URLSearchParams(location.search);['utm_source','utm_medium','utm_campaign','utm_content','utm_term'].forEach(k=>{if(params.get(k))sessionStorage.setItem(k,params.get(k))});
const form=$('#signup-form');
form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return}const payload=Object.fromEntries(new FormData(form));['utm_source','utm_medium','utm_campaign','utm_content','utm_term'].forEach(k=>payload[k]=sessionStorage.getItem(k)||'');payload.createdAt=new Date().toISOString();localStorage.setItem('contact-s-telom:lastLead',JSON.stringify(payload));$('.form-status').textContent='Спасибо! Заявка сохранена. Мы свяжемся с вами, чтобы подтвердить место.';form.reset();window.dataLayer?.push({event:'form_submit'})});
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
